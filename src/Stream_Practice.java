import java.util.*;
import java.util.stream.*;
public class Stream_Practice {
    public static void main(String[] args) {

//        Find the longest string in a list of strings using Java streams:
//        List<String> strings = Arrays.asList("apple", "banana", "cherry", "date", "grapefruit");
//
//        List<String> lager = strings.stream().max(Comparator.comparingInt(String::length)).stream().collect(Collectors.toList());
//
//        System.out.println(lager);

//        Calculate the average of a list using Java streams:
//        List<Integer> persons = Arrays.asList(21,19,1,4,30,29,7,18,45);
//
//        OptionalDouble avg = persons.stream().mapToInt(Integer::intValue).average();
//
//        System.out.println(String.format("%.2f", avg.getAsDouble()));


//        Merge two sorted lists into a single sorted list using Java streams:
//        List<Integer> list1 = Arrays.asList(1, 3, 5, 7, 9);
//        List<Integer> list2 = Arrays.asList(2, 4, 6, 8, 10);
//
//        List<Integer> sortedlist = Stream.concat(list1.stream(),list2.stream()).sorted().collect(Collectors.toList());
//
//        System.out.println(sortedlist);


//        Find the intersection of two lists using Java streams:
//        List<Integer> list1 = Arrays.asList(1, 2, 3, 4, 5,7);
//        List<Integer> list2 = Arrays.asList(3, 4, 5, 6, 7);
//
//        List<Integer> same = list1.stream().filter(list2::contains).collect(Collectors.toList());
//
//        System.out.println(same);


//        Remove duplicates from a list while preserving the order using Java streams:
//        List<String> Duplicates = Arrays.asList("Elango","Kannan","Elango","jackie","Kannan","jackie");
//
//        List<String> unique = Duplicates.stream().distinct().collect(Collectors.toList());
//
//        System.out.println(unique);



    }
}
